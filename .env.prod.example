# ==============================================
# Knok FM Production Environment Configuration
# ==============================================
# Copy this file to .env.prod and fill in your actual values
# IMPORTANT: Never commit .env.prod to git!

# ---------------------------------------------
# Server Configuration
# ---------------------------------------------
PORT=8080
LOG_LEVEL=info

# ---------------------------------------------
# Database Configuration
# ---------------------------------------------
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database?sslmode=disable
DATABASE_URL=postgresql://knokfm:CHANGE_THIS_PASSWORD@postgres:5432/knokfm?sslmode=disable

# PostgreSQL credentials (used by docker-compose)
POSTGRES_DB=knokfm
POSTGRES_USER=knokfm
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# ---------------------------------------------
# Redis Configuration
# ---------------------------------------------
REDIS_URL=redis://redis:6379

# ---------------------------------------------
# Discord Bot Configuration
# ---------------------------------------------
# Get your token from: https://discord.com/developers/applications
DISCORD_TOKEN=your_actual_discord_bot_token_here

# Discord Server/Channel Restrictions (Optional)
# Comma-separated list of Discord server (guild) IDs the bot should respond to
# Leave empty to respond in ALL servers the bot is added to
# Example: DISCORD_ALLOWED_GUILDS=1404225099841667072
DISCORD_ALLOWED_GUILDS=

# Comma-separated list of Discord channel IDs the bot should listen to
# Leave empty to listen in ALL channels
# You can also configure per-server allowed channels via the database settings field
# Example: DISCORD_ALLOWED_CHANNELS=1322276431471968356
DISCORD_ALLOWED_CHANNELS=

# ---------------------------------------------
# Admin API Authentication
# ---------------------------------------------
# Generate a secure key with: openssl rand -hex 32
ADMIN_API_KEY=GENERATE_WITH_openssl_rand_hex_32

# ---------------------------------------------
# Unknown Platform Handling
# ---------------------------------------------
# Values: "permissive" (accept all URLs) or "strict" (reject unknown platforms)
UNKNOWN_PLATFORM_MODE=permissive

# ---------------------------------------------
# Frontend Build Configuration (for Dockerfile.web)
# ---------------------------------------------
# This is injected at build time into the React app
VITE_API_BASE_URL=https://api.knok-fm.com
